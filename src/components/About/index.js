import "./index.scss";
import React from "react";
import Loader from "react-loaders";

const About = () => {
  return (
    <div>
      <Loader type="pacman" />
    </div>
  );
};

export default About;
